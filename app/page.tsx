"use client"
import LoginPageComponent from "@/components/login-page" // Import the actual login page component

// This is the main default export for the page route "/"
export default function Page() {
  return <LoginPageComponent /> // Render the imported login page component
}
